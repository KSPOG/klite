{
  "internalName": "${pluginId}",
  "displayName": "${pluginDisplayName}",
  "version": "${pluginVersion}",
  "author": "${pluginAuthor}",
  "description": "${pluginDescription}",
  "tags": ["klite", "grand-exchange", "flipping", "copilot", "automation"],
  "plugins": ["${pluginMainClass}"]
}
